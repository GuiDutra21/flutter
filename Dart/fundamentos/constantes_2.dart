
main()
{   
      
    final lista = ["Ana", 'Roberto', 'joao'];
    lista.add ('amelia');
    print(lista);
    // Com o final eu posso adicionar e/ou excluir, porem nao posso fazer a variavel receber outro conjunto de valores, 
    // Por exemplo ==> lista = ['artur', 'carlos', 'rebeca'];
    // O programa nao iria aceitar

    var listas = const ['ana', 'joao', 'pedro'];
    // Nesse caso podemos fazer a lista receber um outro conjunto de valores, mas nao podemos alterar as constantes
    // Por exemplo ==> lista.add('kelvin'); isso nao pode
    // Por exemplo ==> lista = ['murilo', 'maria']; isso pode 

}